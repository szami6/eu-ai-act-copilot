"""Prompt builders and their structured-output schemas, one module per LLM-
calling orchestrator node (PLAN.md §2.2 lists `prompts/` as its own package
for the agent, unlike the RAG subgraph's simpler inline prompts).
"""
